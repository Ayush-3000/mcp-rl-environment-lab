[project]
name = "mcp-rl-environment-lab"
version = "0.1.0"
description = "Deterministic coding-agent tasks exposed through local MCP-style tools."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
