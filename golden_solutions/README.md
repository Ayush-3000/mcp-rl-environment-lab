# Golden solutions

Each task fixture is intentionally tiny so the expected outcome is reviewable. `solution.txt` represents the observable produced by the documented reference solution; it is never copied into an agent workspace at runtime.
